PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=170224
PGDATABASE=dayflow

# Used to sign JWT auth tokens - keep this secret, don't share it
JWT_SECRET=dayflow_super_secret_key_2026_change_me
JWT_EXPIRES_IN=8h

PORT=5000

# SMTP settings for sending OTP emails - leave as-is for now.
# Without real values here, OTPs will just print in your terminal,
# which is fine for testing.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
